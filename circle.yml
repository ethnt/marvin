dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get remove clang
    - sudo apt-get install clang-3.5 libclang-common-3.5-dev llvm-3.5-dev llvm-3.5

machine:
  ruby:
    version: 2.3.1

  environment:
    LD_LIBRARY_PATH: /usr/lib/llvm-3.5/lib

  environment:
    CODECLIMATE_REPO_TOKEN: 014ebcd7620996e3cd2cf0f503c42560277ba186c8373308300ac06cf4e9b22b
